DISCORD_TOKEN=your_bot_token_here
CLIENT_ID=your_application_id_here

# Optional: set this to instantly register commands to one server during development.
# Omit it (or leave blank) to register commands globally (up to 1 hour to propagate).
# GUILD_ID=your_test_server_id_here
